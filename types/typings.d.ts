export type WhichSectionIsActive = {
  home: boolean;
  about: boolean;
  projects: boolean;
  contact: boolean;
};

export type SliderData = {
  mainTitle: string;
  additionalInfo: string;
  percentage: number;
};
